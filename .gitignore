# compiled output
dist
tmp
out-tsc

# dependencies
node_modules

# IDEs and editors
/.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# misc
/coverage
npm-debug.log

# Environment / secrets — never commit real .env, only .env.example templates
.env
.env.local
.env.*.local
!.env.example

# Claude Code local artifacts
.claude/

# System files
.DS_Store
Thumbs.db

# Nx
.nx/cache
.nx/workspace-data
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Next.js — `out` is the static export
.next
out
# Rewritten by `next dev` and `next build` on every run (r10c #281), so it is
# generated by the `typegen` target instead of tracked.
next-env.d.ts

test-output
vite.config.*.timestamp*
vitest.config.*.timestamp*

# Playwright
.playwright-mcp
